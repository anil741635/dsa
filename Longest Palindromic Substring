class Solution {
    public String getLongestPal(String s) {
        // code here
        int start=0,end=0;
        for(int i=0;i<s.length();i++){
            int exp1=expandAround(s,i,i);
            int exp2=expandAround(s,i,i+1);
            int len=Math.max(exp1,exp2);
            if(len>end-start+1){
                start=i-(len-1)/2;
                end=i+len/2;
            }
        }
        return s.substring(start,end+1);
    }
    public static int expandAround(String s,int left,int right){
        while(left>=0 && right<s.length() && s.charAt(left)==s.charAt(right)){
            left--;
            right++;
        }
        return right-left-1;
    }
}
