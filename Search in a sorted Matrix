class Solution {
    public boolean searchMatrix(int[][] mat, int x) {
        // code here
        int n=mat.length,m=mat[0].length;
        int i=0,j=m-1;
        while(i<=j){
            int mid=i+(j-i)/2;
            if(mat[mid])
        }
    }
}
