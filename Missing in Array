class Solution {
    int missingNum(int arr[]) {
        // code here
        int n=arr.length+1;
        long sum=(long) n*(n+1)/2;
        long actualSum=0;
        for(int i:arr){
            actualSum+=i;
        }
        return (int) (sum-actualSum);
    }
}
