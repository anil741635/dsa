class Solution {
    public int missingNumber(int[] arr) {
        // code here
        Set<Integer> set=new HashSet<>();
        for(int i:arr){
            set.add(i);
        }
        int missing=1;
        while(set.contains(missing)) missing++;
        return missing;
    }
}
